//===--- DeclNodes.def - Declarations ASTs x-macros -------------*- C++ -*-===//
// Part of the Sora project, licensed under the MIT license.
// See LICENSE.txt in the project root for license information.
//
// Copyright (c) 2019 Pierre van Houtryve
//===----------------------------------------------------------------------===//

#ifndef DECL
# define DECL(KIND, PARENT)
#endif

#ifndef ABSTRACT_DECL
# define ABSTRACT_DECL(KIND, PARENT)
#endif

#ifndef DECL_RANGE
# define DECL_RANGE(KIND, FIRST, LAST)
#endif

//===----------------------------------------------------------------------===//

/*
ABSTRACT_DECL(Named, Decl)
  ABSTRACT_DECL(Value, NamedDecl)
    DECL(Func, ValueDecl)
    // Represents a single variable (contains identifier + TypeLoc)
    // TypeLoc can have no repr and just a type (if inferred type) or
    // both (if explicit type)
    DECL(Var, ValueDecl)
    // Represents a parameter declaration. No link to the PBD whatsoever.
    DECL(Param, ValueDecl)  //
    DECL_RANGE(Value, Func, Param)
  DECL_RANGE(Named, Func, Param)

ABSTRACT_DECL(PatternBinding, Decl)
  DECL(Let, Decl)
  DECL_RANGE(PatternBinding, Let, Let)
*/

//===----------------------------------------------------------------------===//

#undef DECL
#undef ABSTRACT_DECL
#undef DECL_RANGE